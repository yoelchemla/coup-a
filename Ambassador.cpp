#include "Ambassador.hpp"
using namespace std;


namespace coup
{
    Ambassador::Ambassador(Game &g,string name) : Player(g,name){ //constructor
    }

    void Ambassador::transfer(Player P1, Player P2){}
}